package libs

// Test error message
const (
	TEST_HAVE_ERROR                   = "Must have error"
	TEST_HAVE_NO_ERROR                = "Must have no error"
	TEST_CONTAIN_ERROR_MESSAGE        = "Must contain error message"
	TEST_DATA_EQUAL_WITH_EXPECTED     = "Data must equal with expected value"
	TEST_DATA_NOT_EQUAL_WITH_EXPECTED = "Data must not equal with expected value"
	TEST_DATA_GREATER_THAN_EXPECTED   = "Data must greater than expected value"
	TEST_DATA_LESS_THAN_EXPECTED      = "Data must less than expected value"
	TEST_DATA_SAME_TYPE_WITH_EXPECTED = "Data must be the same type with expected value"
	TEST_DATA_EMPTY                   = "Data must be empty"
	TEST_DATA_NOT_EMPTY               = "Data must not be empty"
)
